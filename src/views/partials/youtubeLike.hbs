<div class="sectioncontainer h-entry">
	<div class="u-like-of h-cite">
	<div class="row">
		<span class="left">🧡</span>
		<span class="right">
			<a
				href="{{data.url}}"
				target="_blank"
				rel="noreferrer noopener"
				class="p-name u-url entry-title"
			>
				{{data.title}}
			</a>
			<span class="p-author h-card">by {{data.channel}}</span>
		</span>
	</div>
	</div>

	<div class="row">
		<span class="left"></span>
		<time
			title="{{data.created_at}}"
			datetime="{{data.created_at}}"
			class="right dt-published"
		>
			<small>
				<a class="u-url u-uid" href="/youtube/{{data.id}}">
					{{data.timeago}}
				</a>
			</small>
		</time>
	</div>

	{{> author }}
</div>
