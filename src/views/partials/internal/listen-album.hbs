<form method="POST" action="/listens/albums/{{data.id}}">
	{{> internal/id id=data.id type="music/album" }}

	<input type="hidden" name="artist_id" value="{{artist_id}}"/>

	{{> internal/crudRow emoji="💿" name="album" value=data.album }}
	{{> internal/crudRowNumber emoji="📅" name="release_year" value=data.release_year }}
	{{> internal/crudRow emoji="📦" name="genre" value=data.genre }}

	{{#if data.id}}
	<div class="row">
		<div class="left">🔗</div>
		<div class="right">
			<a href="/listens/tracks?album_id={{data.id}}">
				Edit {{data.trackCount}}
				track{{#xif data.trackCount '!=' 1}}s{{/xif}}
			</a>
		</div>
	</div>
	{{/if}}

	{{> internal/crudSubmit }}
</form>
