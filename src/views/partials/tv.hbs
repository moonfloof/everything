<div class="h-entry">
	<div class="u-watch-of h-cite">
	<div class="row">
		<span class="left">📺</span>
		<span class="right">
			<span class="p-name entry-title">{{data.episode_title}}</span>
		</span>
	</div>
	<div class="row">
		<span class="left"></span>
		<span class="right p-author">{{data.series_title}}</span>
	</div>
	</div>

	<div class="row">
		<span class="left"></span>
		<time
			title="{{data.created_at}}"
			datetime="{{data.created_at}}"
			class="right dt-published"
		>
			<small>
				<a href="/tv/{{data.id}}" class="u-url u-uid">
					{{data.timeago}}
				</a>
			</small>
		</time>
	</div>

	{{> author }}
</div>
